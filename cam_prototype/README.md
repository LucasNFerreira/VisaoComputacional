Para testar essa camera, primeiro instale o Framework SimpleCV.
Após isso dê permissão de execução ao arquivo menu.py. Somente 
pode ser executado em ambiente Linux. Necessita uma webcam

To test the camera, first install the SimpleCV framework.
After that, give to the file menu.py execute permission. This 
aplication only can run on Linux. Requires a webcam.
